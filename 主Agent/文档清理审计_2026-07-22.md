# 文档清理审计（2026-07-22）

## 已执行的低风险清理

- 删除 21 个可由 LaTeX/导出流程重新生成的中间文件：`.aux`、`.blg`、`.log`、`.out`、`.toc`、`.ttt`。保留 `.bbl`，避免在参考文献工具不可用时影响复现。
- 删除两份与 `notes` 目录源文件字节级相同的 `output` 副本：
  - `outputs/019e9e26-a704-79e1-8478-8a7f65a6f8d3/presentations/defense-project/output/defense_brief_en.md`
  - `outputs/019e9e26-a704-79e1-8478-8a7f65a6f8d3/presentations/defense-project/output/experiment_design_review.md`
- 删除两份旧的、字节级相同的 Paper 1 中文审查大纲，保留内容更新的 `..._v05.md`：
  - `主Agent/答辩材料/Paper1_Algorithmic_Authority_and_Information_Calibration_PPT_中文审查大纲.md`
  - `主Agent/答辩材料/Paper1_Algorithmic_Authority_and_Information_Calibration_PPT_中文审查大纲_v04.md`

## 建议直接精简（完全重复，但尚未删除）

1. `主Agent/答辩材料/AI_as_Decision_Architecture_Intro_4-5slides.pptx`
   与 `..._revised.pptx` 字节级相同。建议保留 `revised`，删除无后缀版本。
2. `主Agent/答辩材料/Intro_Paper1_Paper2_Paper3_Combined_MainDeck_v04.pptx`、`v05.pptx`、`v06.pptx`
   三份字节级相同。建议只保留 `v06`；若 `v07` 已确认是后继基准，可三份全部删。
3. `主Agent/答辩材料/Paper3_AI_Responsibility_Design_and_Escalatory_Risk_PPT_final_slides.pptx`
   与 `..._v03.pptx` 字节级相同。建议保留一个；若按版本链管理，保留 `v03`。
4. `tmp/gz_edu_grant/grant_sections_draft.pdf`
   与同目录中文命名的成品 PDF 字节级相同。建议保留中文成品名，删除 draft。

## 明显过时的候选（先由用户确认）

1. `主Agent/答辩材料/Paper1_Double_Bayesian_Distortion_PPT_中文审查大纲.md`
   使用已退出当前主标题/理论预设的 “Double Bayesian Distortion” 口径。建议移入 `archive/旧口径_Paper1/` 或删除。
2. `主Agent/答辩材料/第一章_双重贝叶斯偏离_PPT初版.pptx`
   同属 Paper 1 旧口径初版。建议归档或删除。
3. `outputs/真实多Agent重做版/final/04_研究计划_20轮策略信念任务打磨版.md`
   主控规则明确要求默认不再沿用；beauty contest / strategic belief 任务骨架已被判定为结构性错配。建议归档为“退回版本”，不要放在 `final`。
4. `outputs/真实多Agent重做版/final/02_研究计划_APA7整合版.md`、`02_研究计划_会议答辩整合版.md`
   已被 `02_研究计划_当前基准版_2026-07-04.pdf` 取代。建议移入 `archive/被替代版本/`，避免与当前基准并列。
5. `outputs/人机协作架构研究计划_完整稿.md`、`outputs/人机协作架构研究计划_Agent分工版.md`
   属于第二篇早期复杂协作架构路线，与现行“重复预测/判断中的动态依赖校准”口径偏离。建议归档。
6. `defense_project/latex/` 下 Paper 1 文本与 PDF
   主控已注明仍含旧标题和旧表述。建议暂时保留作为旧稿源，但整体移动到 `archive/旧口径_Paper1_latex/`，不要继续作为现行写作入口。

## 高度雷同、建议合并的候选

1. `修改版本.md` 与 `主Agent/文章项目/01_算法权威与信息校准/当前第一章_算法权威与信息校准_基准稿.md`
   按规范化行比较，较短文档约 99.2% 被后者覆盖，Jaccard 相似度约 98.5%。建议只保留当前基准稿；如需追溯，把 `修改版本.md` 移入归档。
2. Paper 1 中文审查大纲 `v05` 与刚清理的旧大纲约有 98.4% 内容覆盖；现已只保留 `v05`。
3. `outputs/研究计划执行产出/` 与 `outputs/真实多Agent重做版/AgentA|AgentB|AgentC/`
   两套目录围绕同一轮第二篇研究计划拆分产出，主题与职责高度重合。建议以“真实多Agent重做版”为历史主档，前一套整体归档；不要逐文件混合保留。
4. `主Agent/答辩材料/` 中 Combined MainDeck `v01`—`v07`
   除 v04—v06 完全相同外，其余也是连续迭代版本。建议确认最终基准后只保留“最终基准 + 最近一个可回退版本”。
5. Paper 1、Paper 2、Paper 3 的多组 `约10页`、`fixed`、`revised`、`v02`—`v06` PPT
   属连续编辑链，不适合全部放在活跃目录。建议每篇仅保留当前版、上一个稳定版和可复用单页组件，其余归档。

## 结构性建议

- 在 `主Agent/答辩材料/` 下建立 `archive/2026-07-旧版本/`，把已被替代但仍可能追溯的 PPT/Markdown 移入，而不是与现行入口并列。
- 为三篇文章与总答辩分别指定一个明确的 `CURRENT.md` 或统一命名的 `*_CURRENT.pptx`，README 只链接这些入口。
- `tmp/` 中仍有大量渲染截图、拼图、PDF 文本抽取和测试 PPT。它们不是正式文档；确认近期不再渲染后可整批清理，但 `tmp/gz_edu_grant/` 含实质申请书材料，不应与普通临时文件一起删除。
- 根目录的 `download_papers*.py`、`find_papers*.py`、`fix_liu*.py` 是多轮临时脚本版本链，建议另做代码清理，不与本次文档清理混在一起。
