# 实验原型协作指令

## 适用范围与入口

适用于 `E:/Info_AI/projects/paper2-slot-prototype/`。先阅读 `E:/Info_AI/主Agent/AGENTS.md`，再阅读本文件及 README。用户已要求推进具体实验原型；旧项目文件的“不展开 Study”阶段限制不应阻止本次已授权的工程规划。

本目录是原型开发入口；不修改论文研究内容、其他 Paper、共享文献库或主控记忆。用户未要求更新 Codex 记忆文件。

## 不可混淆的要求

- 预测下一轮实际中奖；不得换成最高中奖概率评分。
- jsPsych 为确定前端；后端不绑定平台。
- 支持手机/电脑、二维码/链接、现场/远程，分别记录参与方式与设备。
- α 是待验证行为参数，不能当作信任量表或已经识别的心理机制。
- 研究设计的 FIXED、PROVISIONAL、TBD 见 `docs/DECISIONS.md`。不得将演示用参数改写为用户确认的正式方案。
- 不生成已获伦理批准、已验证 AI 或真实支付完成的虚假说明。

## 工作边界

- 只编辑派发任务中允许的路径。其他目录如需修改，向协调 agent 提出明确接口/文件请求，不顺手修改。
- `sources/` 的快照只读；在当前 `docs/` 修改设计，不反复修改多份历史稿。
- 平台 SDK、网络与认证逻辑只放在适配器或服务器端；jsPsych 插件不直接调用 JATOS、云表格 API。
- 未来开奖结果、私有种子、管理凭证不进入正式网页。演示材料必须标记仅供演示。
- 真实数据、个人信息、令牌不写入仓库。适配器不得把浏览器缓存声称为云端持久保存。

## 并行实施

当前工程已有 G0 0.3.0 接口和单题预览；不要重新搭建脚手架。2026-09-18 用户授权 Codex 接手修复及预览实现的临时 owner 例外见 PARALLEL_TASKS。预览不取代 G1 的 jsPsych 实验或真正 LocalDemo 数据保存。

所有平台的 agent 必须遵守 [第三方 Agent 施工指南](docs/AGENT_CONSTRUCTION_GUIDE.md)。首版手写业务代码统一 TypeScript，具体规范及工具链由该指南约束；不得自行换栈。第一次改代码前创建 `handoffs/<角色>/LATEST.md`，每个小步骤更新；额度不足先保存代码及可取回的交接材料，不能只留聊天总结。恢复时核对实际文件与版本。

多 agent 实施依照 `docs/PARALLEL_TASKS.md`。这次整理仅建立任务划分，不意味着已启动实现。后续实施轮允许按该分工委派独立子任务。

- 先完成 G0 接口和演示配置冻结，再并行编写依赖模块；现在的文档不是 G0 已通过的证据。
- 协调 agent 唯一维护共享类型、根依赖/锁文件、入口装配和任务看板。
- 每个实施 agent 独占自己的文件区及 `handoffs/<角色>/`；不得同时修改共享文件。
- 需要新增依赖时提交名称和用途，由协调 agent 安装；不并发修改锁文件。
- 遇到其他 agent 的在途改动，不回滚、不覆盖。共享工作区只做本任务检查，不运行全仓库清理或 `git add .`。
- 后端替换默认发生在批次之间；不得中途改变已有会话的后端、分组或材料。

## 完成标准

按 `docs/ACCEPTANCE.md` 执行与改动相关的检查。交接报告必须给出修改路径、使用的接口版本、实际执行命令与结果、未测事项和待处理问题。

目录存在、代码可编译、演示可点击、真实后端保存成功、测量有效分别报告，不能相互替代。未运行的测试不写“通过”；不以剩余 token 或时间为理由宣称完成。

## 2026-09-19 当前执行方式

用户最新要求停止 Kimi CLI 和多 agent 调用。后续由 Codex 直接推进，不根据历史并行任务书自动派发 agent。采集入口及存储说明见 docs/COLLECTION_PILOT.md。历史任务/日志仅保留追溯。
