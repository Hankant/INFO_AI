# Codex 界面接手交付

状态：DONE（单题原型范围）。日期：2026-09-18；基线 HEAD 8c2cb3c + MiniMax 未提交增量。

用户授权先核实底层可用性，再由 Codex 接手设计实现；随后指出建议字段写死问题。本轮由同一 Codex 顺序实现与验证，没有启动其他 agent，也不声称独立验收。

## 已完成

1. 保留并复用单题服务的保存回执、事件顺序、幂等和评分。新增显式 adviceForSelf:false 选项，新界面自己判断分支不暴露 AI；旧 preview 默认行为兼容。
2. 重写机柜界面：原创水果 SVG、三机并排、阶段锁定、最终确认后拉杆开奖、确定停止和结果、手机布局。
3. 重写聊天：真实 AsyncIterable 分块消费、停止和同请求继续、保留消息、复制、输入与发送、聊天收起与页面隐藏时取消。独立脚本 ChatService，未来适配器可替换。
4. 把运行时流校验接入页面，核查开始/终结、跨请求、序号、计数、SHA-256、取消位置；修正此前把 schema 校验等同于流校验的注释。
5. 修复建议正确字段常量，添加明确的暴露、建议正确、答案一致及转向建议比较。旧 advice_target_hit 未定义，标记废弃，不能用于分析。评分 0.3.1，新增字段兼容 0.3.0。
6. 新增完整路径测试、正反评分测试和流式反例；README、STATUS、CONTRACTS、PARALLEL_TASKS 已同步。

具体路径、字段计算、记录限制见 [实现说明](../../docs/ATELIER_IMPLEMENTATION.md)。此前检查点中允许的 contracts、domain、adapters、experiment、ui、bootstrap、测试与文档路径均在用户授权范围。其他论文文件未动。

## 验证证据

- npm run test:run：12 文件 / 108 测试通过。
- 历史 Q 两套反例：12/12 与 6/6 通过。
- PLAYWRIGHT_PORT=5198 npm run test:e2e：5/5 通过（旧 preview 2，新界面 3）。5198 用于避开用户已有服务，不终止已有服务。
- npm run lint、npm run build（含类型检查）：通过，无 lint 警告。
- 新增及本轮修改实现已 Prettier 格式化；全项目 npm run format:check 通过。
- 桌面 1280 和手机 390 截图已逐张查看；浏览器另检查实际 5197 页面。360/390 是桌面 Edge 模拟视口，不是真机。
- 初轮手机测试失败是完整 accessible name 包含桌面箭头，手机 CSS 隐藏箭头；改成稳定文字定位后重跑通过。没有用延长超时掩盖故障。

## 未完成与接续

没有完整 jsPsych、多题、远程持久保存、可靠刷新恢复或真实模型。固定建议 B、中奖 C 是模拟素材，不是正式实验控制器；本页答案存在浏览器代码。聊天审计只在本机内存及手动导出 JSON，未声称云端保存。正式 alpha 设计与第二层解释性实验仍需研究决定。

下一执行者先读 AGENTS、施工指南、STATUS 和实现说明；按任务登记路径再接手。优先实施可靠持久保存与完整实验流程，不能把本页直接用于招募。历史 MiniMax 和 Q 报告保持不变。

服务保留 http://127.0.0.1:5197/preview-immersive.html 供用户体验。未提交、推送或部署。
