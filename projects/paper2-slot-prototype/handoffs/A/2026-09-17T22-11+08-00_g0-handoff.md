# G0 交接报告 — 协调 agent A

- **角色 / 实际 agent 标识**：A / mavis (MiniMax Code on win32)
- **任务与允许写入范围**：G0-public-base；仅 `projects/paper2-slot-prototype/` 下 A 允许路径（`package.json`、`package-lock.json`、`tsconfig*.json`、`vite.config.ts`、`vitest.config.ts`、`playwright.config.ts`、`.eslintrc.cjs`、`.prettierrc.json`、`.prettierignore`、`.editorconfig`、`.gitignore`、`README.md`、`AGENTS.md`、`docs/STATUS.md`、`src/contracts/*`、`src/bootstrap.ts`、`src/ui/*`、`config/demo.json`、`config/demo.ts`、`tests/contracts/*`、`tests/fixtures/*`、`tests/config/*`、`tests/e2e/.gitkeep`、`handoffs/A/*`）。G0 例外：可写 `config/demo.json`、可写 `tests/config/*`、可写 `src/ui/*` 演示入口。其余 7+ 项不在 A 路径内的修改均未触碰。
- **读取的 `contract_version` / demo 配置版本**：N/A 起点（DRAFT）；交付时 `contract_version = 0.1.0`、`schema_version = 0.1.0`、`protocol_version = unreleased`、`client_version = 0.1.0`、`demo config = demo-0.1.0`。
- **本次修改文件**：见 `LATEST.md`「代码事实」段汇总。
- **已完成的行为**：
  - `npm install` 一次成功，273 packages，锁到 `package-lock.json`
  - `npm run typecheck / lint / format:check / build / test:run` 全绿
  - `npm run dev` 在本机 294ms ready，HTTP 200 验证
  - `npm run test:e2e` 故意失败（No tests found）— 不复述为通过
- **实际验证命令与结果**：见 `LATEST.md`「验证证据」表 + `docs/STATUS.md`「本轮整理验证」段。
- **未测试 / 未完成**：
  - `preview` 与真机移动端（待 B / Q）
  - 真 JATOS / 真 HTTP 后端（待 C）
  - 真参与者断网 / 刷新 / 完成确认端到端（待 G2 + Q 端到端）
  - Playwright 浏览器安装与 `tests/e2e/*` 用例（属 Q 的 G3 范围，A 不预先安装）
- **接口变更请求**：暂无来自 B/C/D 的请求。
- **风险或研究决定待定项**：`docs/DECISIONS.md` 的 T01–T09 全部未动；`config/demo.json` 只锁定 PROVISIONAL 但已显式标注 SIMULATION，未把候选设置说成研究结论。
- **提交给协调 agent 的下一步**：
  - 如派发 B：以 G0 验收通过的契约为基准写 `src/experiment/*` timeline 与 `src/ui/*` 真实页；遇到契约缺口先在 `handoffs/B/` 申请。
  - 如派发 C：先实现 `src/adapters/local-demo/*` + `src/persistence/*`，再让 Bootstrap 装载真实 `ExperimentAdapter`；`src/bootstrap.ts` 当前 `adapter_wired: false`，由 C 替换。
  - 如派发 D：`config/demo.json` 内容只在通知 A 后接管；擅自修改 demo capability 会破坏真实性能 hard-floor 真相。
  - 如派发 Q：在 `tests/e2e/` 写第一个用例前，请按 `playwright.config.ts` `webServer` 与 `npm install playwright` 流程配置浏览器下载。
- **当前检查点路径 / 任务状态 / 代码取回位置**：
  - 检查点：`handoffs/A/LATEST.md`
  - 完整历史快照（当前）：`handoffs/A/2026-09-17T22-11+08-00_g0-handoff.md`
  - 进度看板：`docs/STATUS.md`
  - 代码取回：`E:/Info_AI/projects/paper2-slot-prototype/`（本机已落盘）
  - 状态：`DONE`

## B / C / D 可启动的具体条件

- **`contract_version = 0.1.0` 已在 `src/contracts/index.ts` 公开**；B/C/D import 路径统一为 `from 'src/contracts/index.js'`。
- **公共类型**：`SessionService` / `TrialService` / `ResultStore` / `ExperimentAdapter`、`Session` / `ExperimentState` / `PublicTrial` / `TrialFeedback` / `SaveReceipt` / `EventEnvelope`、`ContractError`、`CapabilityMap`、`ParticipationMode` / `DeviceClass` / `ExperimentPhase` / `ExperimentEventType` / `PersistenceScope` / `SessionStatus`。
- **运行时校验**：所有公共数据类型都有对应 `*Schema`（zod）。任何 `unknown` 进入公共边界必须先 `schema.safeParse`。
- **错误码**：`ERROR_CODES` 9 项，错误对象形如 `{ code, message, retryable, details? }`，已发到 `tests/contracts/errors.spec.ts`。
- **demo 配置**：`config/demo.json` 中标 `simulation: true`；`config/demo.ts` 抛错（`config_version` drift 或 simulation=false）。B/C/D 不要把 demo.json 当作研究默认值引用；研究默认值待 T01–T09 落定后另开正式配置。

## 已知工程纪律

- **不调用 jsPsych SDK、jatos.\*、fetch** 在 `src/contracts/` 与 `tests/contracts/`；它们只出现在 `src/experiment/`、`src/adapters/`。
- **不引入 React / Vue** 在 UI 层；G0 演示页用原生 HTML + TS；B 的真实实验页继续用原生 HTML。
- **不写「real AI advice」「real backend」字样**；`local-demo` 不应自封 supported 的 idempotentWrites / serverControlledTrials。
- **不要在没有许可时跑 `npm install package`** 加新依赖；改 `package.json` + lockfile 由 A 一次完成，再写 `chore(deps): …` style 的提交由具备权限的人执行。
- **不要把 `sources/` 内容当施工依据**；它们只是 DRAFT 快照，MANIFEST.json 中的 SHA256 仅用于校验拷贝完整。
